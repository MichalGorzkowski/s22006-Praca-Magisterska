//
//  UserInteractionManager.swift
//  Bricks Collection Center
//
//  Created by Michał Gorzkowski on 26/08/2024.
//

// plik przygotowany do załadowania modelu ML

/*

import Foundation

class UserInteractionManager {
    private let firebaseManager = FirebaseManager()
    private let recommendationEngine = MLRecommendationEngine()
    
    func saveUserInteraction(interaction: UserInteraction) {
        firebaseManager.saveUserInteraction(interaction: interaction)
    }
    
    func getUserInteractions(userId: String, completion: @escaping ([UserInteraction]) -> Void) {
        firebaseManager.getUserInteractions(userId: userId, completion: completion)
    }
    
    func getRecommendations(for userId: String, completion: @escaping ([LegoSet]) -> Void) {
        recommendationEngine.getRecommendations(for: userId) { recommendedSets in
            completion(recommendedSets)
        }
    }
}

*/
